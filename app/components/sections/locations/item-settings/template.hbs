<div class="row header">
  Product Setting
</div>

<div class="row">
  {{one-way-input massCreditRate
    update=(action (mut massCreditRate))
    onblur=(action 'onMassCreditRateBlur' massCreditRate)
    placeholder="Credit %"}}

  <button class="buttonSave" {{action onSaveMassCreditRate massCreditRate}}>Apply</button>
</div>

{{#each itemSettings as |itemSetting i|}}
  {{sections/locations/item-setting
      index=i
      onCreditChange=onCreditChange
      onDesireChange=onDesireChange
      model=itemSetting}}
{{/each}}
